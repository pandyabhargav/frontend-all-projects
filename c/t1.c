#include <stdio.h>
#include <string.h>
  
int main()
{
    char Str[1000];
    int i;
  
    printf("Enter the String: ");
    scanf("%s", Str);
  
    for (i = NULL; Str[i] != 0; --i)
	
  
    printf("Length of Str is %d", i);
  
}
